{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "func",
      "label": "backend: start-functions-host",
      "command": "host start",
      "problemMatcher": "$func-node-watch",
      "isBackground": true,
      "dependsOn": "backend: install-functions-deps",
      "options": {
        "cwd": "${workspaceFolder}/api"
      }
    },
    {
      "type": "shell",
      "label": "backend: install-functions-deps",
      "command": "npm install",
      "options": {
        "cwd": "${workspaceFolder}/api"
      }
    },
    {
      "type": "shell",
      "label": "backend: prune-functions-deps",
      "command": "npm prune --production",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}/api"
      }
    },
    {
      "type": "shell",
      "label": "frontend: start-dev-server",
      "command": "npm run dev",
      "isBackground": true,
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": {
        "pattern": [
          {
            "regexp": ".*",
            "file": 1,
            "location": 2,
            "message": 3
          }
        ],
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".*",
          "endsPattern": "ready in .*ms"
        }
      }
    },
    {
      "type": "shell",
      "label": "backend: build-functions",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/api"
      },
      "problemMatcher": []
    },
    {
      "label": "backend: predeploy-compound",
      "dependsOn": ["backend: build-functions", "backend: install-functions-deps", "backend: prune-functions-deps"],
      "dependsOrder": "sequence"
    }
  ]
}
